{
	"name": "mindmap-view",
	"displayName": "Mind Map View",
	"description": "Mind mapping custom editor for markdown files, with true bidirectional sync.",
	"version": "0.0.1",
	"publisher": "TODO-set-publisher-id",
	"private": true,
	"license": "MIT",
	"engines": {
		"vscode": "^1.85.0"
	},
	"categories": [
		"Visualization",
		"Other"
	],
	"capabilities": {
		"untrustedWorkspaces": {
			"supported": true,
			"restrictedConfigurations": [
				"mindmapView.pastedImageFolder"
			]
		},
		"virtualWorkspaces": {
			"supported": "limited",
			"description": "Opening and bidirectionally editing a markdown file as a mind map works over any workspace filesystem. Clicking a link/image, displaying an image thumbnail, and pasting a clipboard image resolve paths relative to the document's own folder using Node's path module against document.uri.fsPath, which assumes a real (file://-shaped) path — these three features may resolve incorrectly or fail on a non-file:// virtual filesystem (e.g. github.dev, vscode-vfs://)."
		}
	},
	"main": "./dist/extension.js",
	"activationEvents": [],
	"contributes": {
		"customEditors": [
			{
				"viewType": "mindmapView.editor",
				"displayName": "Mind Map",
				"selector": [
					{
						"filenamePattern": "*.md"
					}
				],
				"priority": "option"
			}
		],
		"commands": [
			{
				"command": "mindmapView.openAsMindMap",
				"title": "Open as Mind Map"
			},
			{
				"command": "mindmapView.undo",
				"title": "Mind Map: Undo"
			},
			{
				"command": "mindmapView.redo",
				"title": "Mind Map: Redo"
			},
			{
				"command": "mindmapView.search",
				"title": "Mind Map: Search"
			},
			{
				"command": "mindmapView.rebalance",
				"title": "Mind Map: Rebalance"
			},
			{
				"command": "mindmapView.linkEditor",
				"title": "Mind Map: Edit Link"
			},
			{
				"command": "mindmapView.toggleFold",
				"title": "Mind Map: Toggle Fold"
			},
			{
				"command": "mindmapView.toggleStatusDone",
				"title": "Mind Map: Toggle 'Done' Status"
			},
			{
				"command": "mindmapView.statusQuickPick",
				"title": "Mind Map: Set Node Status..."
			},
			{
				"command": "mindmapView.goToSection",
				"title": "Mind Map: Go to Note Section"
			},
			{
				"command": "mindmapView.toggleToText",
				"title": "Mind Map: Switch to Markdown Editor"
			},
			{
				"command": "mindmapView.toggleToMindMap",
				"title": "Mind Map: Switch to Mind Map Editor"
			}
		],
		"keybindings": [
			{
				"command": "mindmapView.undo",
				"key": "ctrl+z",
				"mac": "cmd+z",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.redo",
				"key": "ctrl+shift+z",
				"mac": "cmd+shift+z",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.redo",
				"key": "ctrl+y",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.search",
				"key": "ctrl+f",
				"mac": "cmd+f",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.rebalance",
				"key": "ctrl+shift+b",
				"mac": "cmd+shift+b",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.linkEditor",
				"key": "ctrl+k",
				"mac": "cmd+k",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.toggleFold",
				"key": "ctrl+/",
				"mac": "cmd+/",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.toggleStatusDone",
				"key": "ctrl+shift+d",
				"mac": "cmd+shift+d",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.statusQuickPick",
				"key": "ctrl+shift+i",
				"mac": "cmd+shift+i",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.goToSection",
				"key": "ctrl+shift+g",
				"mac": "cmd+shift+g",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.toggleToText",
				"key": "ctrl+m",
				"mac": "cmd+m",
				"when": "activeCustomEditorId == 'mindmapView.editor'"
			},
			{
				"command": "mindmapView.toggleToMindMap",
				"key": "ctrl+m",
				"mac": "cmd+m",
				"when": "editorTextFocus && editorLangId == 'markdown'"
			}
		],
		"configuration": {
			"title": "Mind Map View",
			"properties": {
				"mindmapView.writeDebounceMs": {
					"type": "number",
					"default": 400,
					"minimum": 100,
					"maximum": 5000,
					"description": "How long to wait (ms) after your last edit in the map before writing it back to the file. Takes effect immediately for already-open maps."
				},
				"mindmapView.externalEditForwardDebounceMs": {
					"type": "number",
					"default": 300,
					"minimum": 0,
					"maximum": 5000,
					"description": "How long to wait (ms) after an external change to the file (e.g. edited in a split text editor) before refreshing an open map. Takes effect immediately for already-open maps."
				},
				"mindmapView.animationNodeThreshold": {
					"type": "number",
					"default": 500,
					"minimum": 50,
					"maximum": 10000,
					"description": "Above this many visible nodes, fold/unfold position animations turn off and changes apply instantly. Takes effect the next time a map is opened."
				},
				"mindmapView.headingDepth": {
					"type": "number",
					"default": 1,
					"minimum": 0,
					"maximum": 5,
					"description": "Nodes at or above this depth are written as markdown headings; deeper nodes become list items. Root is depth 0 (always H1); the default (1) means first-level branches are H2. Takes effect the next time a map is opened."
				},
				"mindmapView.layoutMode": {
					"type": "string",
					"enum": ["balanced", "right-only", "left-only"],
					"enumDescriptions": ["Balance first-level branches left and right of the root", "Place all first-level branches to the right of the root", "Place all first-level branches to the left of the root"],
					"default": "balanced",
					"description": "How first-level branches are arranged around the root. Takes effect the next time a map is opened."
				},
				"mindmapView.pastedImageFolder": {
					"type": "string",
					"default": "",
					"description": "Folder, relative to the markdown file's own folder, where images pasted from the clipboard into the map are saved. Empty (the default) saves them alongside the file. Example: \"assets\". Read fresh on each paste, so it takes effect immediately."
				},
				"mindmapView.showRelations": {
					"type": "boolean",
					"default": true,
					"description": "Show same-document relation arrows and cross-document relation badges. Takes effect the next time a map is opened."
				}
			}
		}
	},
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -p tsconfig.json && tsc -noEmit -p tsconfig.webview.json && node esbuild.config.mjs production",
		"fixtures": "node scripts/generate-fixtures.mjs",
		"bench:m1": "node scripts/bench-m1.mjs",
		"bench:open": "node scripts/bench-open.mjs",
		"bench:m2": "node scripts/bench-m2.mjs",
		"bench:images": "node scripts/bench-images.mjs",
		"bench:relations": "node scripts/bench-relations.mjs",
		"test": "vitest run",
		"test:watch": "vitest",
		"package": "npm run build && npx @vscode/vsce package --no-rewrite-relative-links"
	},
	"devDependencies": {
		"@types/node": "^20.11.0",
		"@types/vscode": "^1.85.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.28.1",
		"jsdom": "^29.1.1",
		"typescript": "^5.3.3",
		"vitest": "^3.2.5"
	},
	"dependencies": {
		"d3-flextree": "^2.1.2"
	}
}
